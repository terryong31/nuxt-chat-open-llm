# finetune — LoRA recipes for the local checkpoint

Training data and commands for teaching `mlx-community/Mamba-Codestral-7B-v0.1-4bit`
to emit tool calls reliably. See `docs/adr/0007` for why that is needed.
